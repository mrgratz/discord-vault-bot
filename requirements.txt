# Core
discord.py[voice]>=2.0
python-dotenv>=1.0
edge-tts>=6.1

# Voice pipeline (required for /voice command)
discord-ext-voice-recv>=0.6
groq>=0.4
davey>=0.1
torch>=2.0
silero-vad>=5.0

# Python 3.13 removed audioop — install the backport if needed:
# pip install audioop-lts

# Optional — only needed for /reel command (Instagram transcription):
# openai-whisper
# yt-dlp
